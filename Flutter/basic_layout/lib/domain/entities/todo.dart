class Todo {
  final String title;
  final String description;
  final String time;
  final bool done;
  final int id;
  Todo({
    required this.title,
    required this.description,
    required this.time,
    required this.done,
    required this.id,
  });
}
